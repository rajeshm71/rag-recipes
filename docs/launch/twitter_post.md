# X / Twitter launch draft

**Status: TEMPLATE. Do not post as-is.** The bracketed line needs the real leaderboard winner and
number, which requires a real-key run -- not yet done as of this draft. Everything else is ready.

---

I benchmarked 10 RAG patterns on the same corpus and eval set. Here's what won:

[FILL IN once the real-key run lands: winning pattern + its hit@10 / cost tradeoff, e.g.
"Hybrid+rerank wins on hit@10 (0.XX), but pattern 04 alone gets 90% of the benefit at half the
latency."]

10 patterns, 2 baselines, 2 appendix studies (chunking, embedding swap), all held to the same
eval set with 95% CIs. Every pattern is also a copy-paste-able Python function, not a framework.

No LangChain. No black box. Full leaderboard + failure-mode analysis for every pattern below 👇

(link in reply)
