# Required for real notebook runs (NOT for CI)
OPENAI_API_KEY=sk-...

# Optional; only needed for pattern 07 (contextual retrieval), which uses
# Anthropic specifically for prompt-cache economics
ANTHROPIC_API_KEY=

# Optional; only needed for A2_embedding_swap.ipynb
VOYAGE_API_KEY=

# Force mock LLM. Set automatically by CI. Leave as "openai" for real runs.
RAG_RECIPES_LLM=openai
